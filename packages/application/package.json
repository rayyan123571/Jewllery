{
  "name": "@jewellery/application",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "comment": "Business calculations. Depends on domain only. No SQL, no Electron, no React — that is what makes every calculation testable with no database and no window.",
  "dependencies": {
    "@jewellery/domain": "*"
  }
}
